server:
  port: 8888
spring:
  application:
    name: microservicio-config
  profile:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs

